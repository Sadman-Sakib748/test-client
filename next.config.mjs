/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "thumbs.dreamstime.com", "api.genesiscarpenter.com", "i.ibb.co"],
  },
};

export default nextConfig;
