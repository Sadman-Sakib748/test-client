/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["localhost", "thumbs.dreamstime.com", "api.genesiscarpenter.com", "ibb.co.com"],
  },
};

export default nextConfig;
